{
  "name": "star-wars-trivia-ai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Star Wars Trivia AI — an infinite, level-based Star Wars trivia game powered by SWAPI, hosted on Cloudflare Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-data": "node scripts/fetch-swapi.mjs",
    "deploy": "vite build && wrangler pages deploy dist --project-name star-wars-trivia-ai"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
